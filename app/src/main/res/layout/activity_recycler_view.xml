<?xml version="1.0" encoding="utf-8"?>
<!-- 
    RecyclerView Activity 布局
    使用 ConstraintLayout 布局，包含：
    1. 标题 TextView
    2. RecyclerView 列表
    3. 添加按钮（FAB）
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".RecyclerViewActivity">

    <!-- 标题 -->
    <TextView
        android:id="@+id/titleText"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:text="RecyclerView 示例：用户列表"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- 
        RecyclerView - 核心组件
        约束要点：
        - top_toBottomOf="@id/titleText": 位于标题下方
        - bottom_toBottomOf="parent": 底部对齐父容器（填充满剩余空间）
        - start/end: 左右对齐父容器
        - width/height="0dp": 在约束范围内填充
    -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:padding="8dp"
        android:clipToPadding="false"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/titleText"
        tools:listitem="@layout/item_user" />

    <!-- 
        浮动操作按钮（FAB）- 用于添加新项
        约束要点：
        - bottom_toBottomOf="parent": 底部对齐父容器
        - end_toEndOf="parent": 右边对齐父容器
        - margin: 保持 16dp 边距
    -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabAdd"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:contentDescription="添加用户"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:srcCompat="@android:drawable/ic_input_add" />

</androidx.constraintlayout.widget.ConstraintLayout>

